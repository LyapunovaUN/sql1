# Лабораторная работа №1  

**Номер варианта:** 10  

---

## Задание 1.1  
Вывести дилеров (dealerships) с широтой (latitude) > 40.  
Сортировка: широта.

**Изменения:**  
Изменения не вносились. Запрос полностью соответствует условию задания.

📷 Скриншот выполнения SELECT (из основной БД)
<img width="1694" height="446" alt="image" src="https://github.com/user-attachments/assets/50869c7b-0cca-4d67-9022-72439c6fbf30" />

---

## Задание 1.2  
Вывести товары (products) типа 'automobile' с ценой < 15000.

**Изменения:**  
Порог цены был изменён с 15000 на 90000, так как при исходном условии выборка  была пустой.  
Изменение позволило получить более наглядный и содержательный результат выборки.

📷 Скриншот выполнения SELECT (из основной БД)
<img width="1911" height="347" alt="image" src="https://github.com/user-attachments/assets/f90252c5-01eb-4dd2-82ef-fd9f6e6b3dc4" />

---

## Задание 1.3  
Создать таблицу `cheap_parts` (товары с ценой < 50).  
Удвоить цену.  
Удалить тип 'accessory' (если есть).

**Изменения:**  

1. Условие отбора было изменено с `base_msrp < 50` на `base_msrp < 600`,  
так как в исходной таблице отсутствуют товары с ценой менее 50.

2. Удаление типа `'accessory'` формально выполнено, однако фактически в таблице такие записи отсутствуют, поэтому операция не оказывает влияния на результат.

📷 Скриншот подтверждения CRUD (итоговая выборка из таблицы `cheap_parts` после всех изменений)
<img width="1177" height="566" alt="image" src="https://github.com/user-attachments/assets/409bbc5c-998e-4b28-a842-fae19304a366" />


---

# Вывод

В ходе выполнения лабораторной работы были реализованы выборки данных с условиями фильтрации и сортировки, а также выполнены CRUD-операции: создание таблицы, обновление и удаление данных.

В процессе работы были внесены изменения в условия заданий, обусловленные особенностями исходной базы данных (отсутствие нужных значений и слишком малые выборки).  
Все результаты корректны и подтверждены скриншотами.

Файл `lab_01.sql` содержит полный набор SQL-запросов.


# Лабораторная работа №2  

**Номер варианта:** 10  

---

# Задания

## Задание 2.1  
Показать все продажи модели 'Model Chi' с именами покупателей.

### Пояснение

Используется `INNER JOIN`, так как необходимо получить только те записи, для которых есть соответствия во всех таблицах (`sales`, `customers`, `products`).
Соединение выполняется по ключам `customer_id` и `product_id`.
Такой подход позволяет связать продажи с конкретными покупателями и товарами и исключить несвязанные записи.

📷 Скриншот результата (первые 10–15 строк)
<img width="1843" height="846" alt="image" src="https://github.com/user-attachments/assets/3e3be781-a5ee-4aa3-9ce0-ac22108cf9af" />

---

## Задание 2.2

Найти сотрудников, нанятых позже, чем самый старый сотрудник был уволен.


### Пояснение

В данном запросе используется подзапрос, который находит минимальную дату увольнения (`MIN(termination_date)`), исключая NULL-значения.
Основной запрос затем выбирает сотрудников, принятых на работу позже этой даты.
JOIN здесь не требуется, так как сравнение происходит внутри одной таблицы.

📷 Скриншот результата (первые 10–15 строк)
<img width="1201" height="833" alt="image" src="https://github.com/user-attachments/assets/60081c2d-3f60-4b67-a677-290a91630727" />

---

## Задание 2.3

Используя COALESCE, в таблице dealerships вывести дату закрытия.
Если дилерский центр не закрыт — вывести 'Still Open'.


### Пояснение

Перед выполнением изменений была создана копия таблицы (dealerships_02), чтобы избежать случайного изменения или повреждения исходных данных в таблице dealerships.

Функция `COALESCE` возвращает первое значение, которое не равно NULL.
В данном случае:

* если `date_closed` заполнено — выводится дата закрытия;
* если значение отсутствует (NULL) — подставляется строка `'Still Open'`.

Это позволяет корректно обработать отсутствующие значения и сделать результат более понятным.

📷 Скриншот результата (первые 10–15 строк)
<img width="1302" height="1085" alt="image" src="https://github.com/user-attachments/assets/53595e77-505e-4799-8594-fe8cf96bf389" />

---

# Вывод

В ходе выполнения лабораторной работы были изучены:

* объединение таблиц с помощью `INNER JOIN`;
* использование подзапросов для выборки данных;
* обработка NULL-значений с помощью функции `COALESCE`.

Файл `lab_02.sql` содержит полный набор SQL-запросов.

# Лабораторная работа №3  

**Номер варианта:** 10  

---

# Задания

## Задание 3.1  
Найти дисперсию (VAR_SAMP) цены продуктов.

### Пояснение  
Для решения использована агрегатная функция `VAR_SAMP`, которая вычисляет выборочную дисперсию значений.  
В расчёт включены только те записи, где цена (`base_msrp`) не равна NULL, чтобы избежать искажения результата.  

Дополнительно результат был округлён до двух знаков после запятой для более удобного отображения.

📷 Скриншот результата

<img width="341" height="170" alt="image" src="https://github.com/user-attachments/assets/86362668-89f6-46a6-92c1-3198e88e26a2" />


---

## Задание 3.2  
Посчитать количество дилеров в каждом штате.

### Пояснение  
Используется группировка (`GROUP BY`) по полю `state`, что позволяет объединить дилеров по штатам.  
Функция `COUNT(*)` подсчитывает количество записей в каждой группе.  

Также добавлена сортировка по убыванию количества дилеров, чтобы наиболее представленные штаты отображались первыми.

📷 Скриншот результата (первые 10–15 строк)

<img width="488" height="873" alt="image" src="https://github.com/user-attachments/assets/3efd0e5e-70b9-422a-bb9c-f1d0fa73f526" />

---

## Задание 3.3  
Вывести штаты, где суммарная выручка превысила 1,000,000.

### Пояснение  
В данном запросе используется `LEFT JOIN` для объединения таблиц `dealerships` и `sales`.  
Это позволяет учитывать все дилерские центры, даже если у некоторых из них нет продаж.  

Далее выполняется:
- группировка по штатам (`GROUP BY state`);
- вычисление суммарной выручки с помощью `SUM(sales_amount)`;
- фильтрация групп с помощью `HAVING`, так как условие применяется к агрегированным данным.  

Использование `HAVING` необходимо, поскольку обычный `WHERE` не работает с агрегатными функциями.

📷 Скриншот результата (первые 10–15 строк)

<img width="467" height="886" alt="image" src="https://github.com/user-attachments/assets/5226ce42-d037-487b-93d1-7ccc1734752d" />

---

# Вывод  

В ходе выполнения лабораторной работы были изучены:
- агрегатные функции (`VAR_SAMP`, `COUNT`, `SUM`);
- группировка данных с помощью `GROUP BY`;
- фильтрация агрегированных результатов через `HAVING`;
- использование `LEFT JOIN` для сохранения всех записей из основной таблицы.  

Файл `lab_03.sql` содержит полный набор SQL-запросов.



