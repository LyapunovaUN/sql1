````md
# Лабораторная работа №2  

**Номер варианта:** 10  

---

# Задания

## Задание 2.1  
Показать все продажи модели 'Model Chi' с именами покупателей.

### SQL-запрос
```sql
SELECT 
    p.model,
    c.first_name,
    c.last_name,
    c.email,
    s.sales_transaction_date,
    s.sales_amount,
    s.channel
FROM sales s
INNER JOIN customers c ON s.customer_id = c.customer_id
INNER JOIN products p ON s.product_id = p.product_id
WHERE p.model = 'Model Chi'
ORDER BY s.sales_transaction_date DESC;
```

### Пояснение

Используется `INNER JOIN`, так как необходимо получить только те записи, для которых есть соответствия во всех таблицах (`sales`, `customers`, `products`).
Соединение выполняется по ключам `customer_id` и `product_id`.
Такой подход позволяет связать продажи с конкретными покупателями и товарами и исключить несвязанные записи.

📷 Скриншот результата (первые 10–15 строк)
<img width="1843" height="846" alt="image" src="https://github.com/user-attachments/assets/3e3be781-a5ee-4aa3-9ce0-ac22108cf9af" />

---

## Задание 2.2

Найти сотрудников, нанятых позже, чем самый старый сотрудник был уволен.

### SQL-запрос

```sql
SELECT 
    sp.first_name,
    sp.last_name,
    sp.hire_date,
    sp.termination_date
FROM salespeople sp
WHERE sp.hire_date > (
    SELECT MIN(sp2.termination_date) 
    FROM salespeople sp2
    WHERE sp2.termination_date IS NOT NULL
)
ORDER BY sp.hire_date;
```

### Пояснение

В данном запросе используется подзапрос, который находит минимальную дату увольнения (`MIN(termination_date)`), исключая NULL-значения.
Основной запрос затем выбирает сотрудников, принятых на работу позже этой даты.
JOIN здесь не требуется, так как сравнение происходит внутри одной таблицы.

📷 Скриншот результата (первые 10–15 строк)
<img width="1201" height="833" alt="image" src="https://github.com/user-attachments/assets/60081c2d-3f60-4b67-a677-290a91630727" />

---

## Задание 2.3

Используя COALESCE, в таблице dealerships вывести дату закрытия.
Если дилерский центр не закрыт — вывести 'Still Open'.

### SQL-запрос

```sql
CREATE TABLE dealerships_02 AS
SELECT * FROM dealerships;

ALTER TABLE dealerships_02
ADD COLUMN closing_status TEXT;

UPDATE dealerships_02
SET closing_status = COALESCE(date_closed::TEXT, 'Still Open');

SELECT 
    d.street_address,
    d.city,
    d.state,
    d.closing_status,
    d.date_closed
FROM dealerships_02 d
ORDER BY 
    CASE WHEN d.date_closed IS NULL THEN 0 ELSE 1 END,
    d.date_closed DESC;
```

### Пояснение

Перед выполнением изменений была создана копия таблицы (dealerships_02), чтобы избежать случайного изменения или повреждения исходных данных в таблице dealerships.

Функция `COALESCE` возвращает первое значение, которое не равно NULL.
В данном случае:

* если `date_closed` заполнено — выводится дата закрытия;
* если значение отсутствует (NULL) — подставляется строка `'Still Open'`.

Это позволяет корректно обработать отсутствующие значения и сделать результат более понятным.

📷 Скриншот результата (первые 10–15 строк)
<img width="1302" height="1085" alt="image" src="https://github.com/user-attachments/assets/53595e77-505e-4799-8594-fe8cf96bf389" />

---

# Вывод

В ходе выполнения лабораторной работы были изучены:

* объединение таблиц с помощью `INNER JOIN`;
* использование подзапросов для выборки данных;
* обработка NULL-значений с помощью функции `COALESCE`.

Все запросы выполнены корректно и соответствуют условиям заданий.

```
```
